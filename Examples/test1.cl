#include <stdio.h>

// Create a function
void myFunction(int myNumbers[5], char isOdd) {
  for (int i = 0; i < 5; i++) {
  if(isOdd == "y"){
    if(x == y){
     printf("%d\n", myNumbers[i]);
       int temp = 57;
    }
  }
  }
}

int main() {
  myFunction(v1, v2); // call the function
  int myNumbers[5] = { 10, 3, 6, 4, 2};
  return 0;
}